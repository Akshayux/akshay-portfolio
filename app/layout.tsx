import type {Metadata} from 'next';import './globals.css';export const metadata:Metadata={title:'Alex Morgan — Product Designer',description:'Independent product designer and UX strategist creating thoughtful digital experiences.'};export default function RootLayout({children}:Readonly<{children:React.ReactNode}>){return <html lang="en"><body>{children}</body></html>}
